import Position
import Ground

def bubbleSortX(x):
	size = get_world_size()
	for i in range(size):
		for j in range(size - i - 1):
			if(measure(North) < measure()):
				swap(North)
			move(North)
		Position.goTo(Position.getPos(x,0))

Ground.harvestAll()